# نقشه‌ی اجرای فازبندی‌شده — پرامپت‌های اصلاح و توسعه

این سند، اجرای پرامپت‌های پوشه‌ی «پرامپت‌های اصلاح و توسعه» و `FIX_PROMPT.md` را
به فازهای قابل‌تحویل و (تا حد ممکن) قابل‌تست تقسیم می‌کند.

> محدودیت محیط: این سندباکس JDK/Android SDK ندارد، پس اپ اندروید اینجا **کامپایل**
> نمی‌شود. اما توابع Node، اسکیمای `appwrite.json`، محتوای seed و تست‌های
> `backend/functions/tests` واقعاً اجرا و بررسی می‌شوند. کد Kotlin/Compose مطابق
> الگوهای موجود نوشته می‌شود و باید روی سیستمی با Android SDK بیلد شود.

## فاز ۱ — زیرساخت چند-مدلی AI + توابع سرور  ✅ قابل تست همین‌جا
- کالکشن `ai_providers` (فایل ۰۴).
- ماژول مشترک انتخاب provider با fallback (`provider.js` در هر تابع AI).
- بازنویسی `ai-companion` برای خواندن provider فعال از دیتابیس + لاگ ساختاریافته.
- توابع جدید: `generate-recipe` (۰۳)، `generate-move-image` (۰۲)،
  `generate-sketch-reference` (۰۲)، `weekly-plan-engine` (۰۷).
- افزودن سناریوهای تست به `backend/functions/tests/run-tests.js`.

## فاز ۲ — اسکیما + محتوای seed  ✅ قابل تست همین‌جا
- کالکشن‌های تازه در `appwrite.json`: `wellness_moves`، `lesson_media_progress`،
  `lesson_prerequisites`، `exams`، `exam_results`، `weekly_schedule`،
  `review_plans`، `sketch_references`، `move_images`، `ai_providers`.
- توسعه‌ی `content.json`: فهرست کامل یوگا/ورزش/تنفس/تکنیک‌های یادگیری (۰۲)،
  پیش‌نیازها (۰۶)، آزمون‌های بازه‌ای (۰۷).
- به‌روزرسانی `backend/seed/import.js` و `TableIds.kt`/`FunctionIds.kt`.

## فاز ۳ — پلیر رسانه با حافظه و سینک (۰۱)  ⚙️ Kotlin
- کالکشن `lesson_media_progress`، مدل و ریپازیتوری سینک، نوار وضعیت درس.

## فاز ۴ — ماژول ورزش/یوگا/تنفس + مرجع نقاشی (۰۲)  ⚙️ Kotlin
- بارگذاری فهرست کامل، تصویرِ چهره‌ی کاربر، شمارنده‌ی صوتی، بخش «مرجع نقاشی».

## فاز ۵ — آشپزی با تولید دستور (۰۳)  ⚙️ Kotlin
- فیلد جستجو → کش/دیتابیس → تابع `generate-recipe`.

## فاز ۶ — لاگین گوگل + همراه + تنظیمات چند-مدلی (۰۴)  ⚙️ Kotlin
- رفع callback لاگین، صفحه‌ی «مدل‌های هوش مصنوعی» در تنظیمات.

## فاز ۷ — پیش‌نیاز و جمع‌بندی ابتدای درس (۰۶)  ⚙️ Kotlin
- کارت آکاردئونی پیش‌نیاز + ثبت دیده‌شدن.

## فاز ۸ — آزمون بازه‌ای، بازخورد، برنامه‌ی هفتگی (۰۷)  ⚙️ Kotlin
- آزمون، صفحه‌ی نکات ضعف، برنامه‌ی مرور از `weekly-plan-engine`.

## فاز ۹ — قالب استاندارد دروس آینده (۰۵) + FIX_PROMPT
- مستندسازی قالب seed، اجرای `verify.js`، به‌روزرسانی `AUDIT.md`.

هر فاز جداگانه commit می‌شود (بدون push، طبق درخواست کاربر).
