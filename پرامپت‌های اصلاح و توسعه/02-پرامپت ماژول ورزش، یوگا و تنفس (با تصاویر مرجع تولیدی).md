# پرامپت: تکمیل ماژول ورزش/یوگا/تنفس + تصاویر مرجع اختصاصی + آموزش نقاشی سیاه‌قلم

## هدف کلی
لیست فعلی حرکات یوگا/ورزشی/تنفسی کم و ناقص است. باید:
1. تعداد و تنوع حرکات زیاد شود (لیست کامل پیشنهادی پایین آمده).
2. هر حرکت مصور شود؛ اما تصویر باید **تولیدی (AI-generated)** و بر اساس یک **عکس مرجع از چهره‌ی کاربر** باشد (کاربر یک سلفی آپلود می‌کند، تصویر حرکت با همان چهره جنریت می‌شود) — نه استفاده از عکس‌های آماده‌ی اینترنتی.
3. یک ایجنت طراح مجزا («ایجنت طراح تصاویر مرجع») اضافه شود که تصاویر مرجع نقاشی سیاه‌قلم (Pencil Sketch Reference) از نیمه‌حرفه‌ای تا حرفه‌ای (سطح ۴ تا ۱۰ از ۱۰) تولید می‌کند.
4. شمارنده‌های تنفس و تکرار حرکات، اعلان صوتی مشخص داشته باشند.

## ۱) ساختار داده‌ی حرکت (Appwrite Collection: `wellness_moves`)
```json
{
  "id": "string",
  "category": "yoga|exercise|breathing",
  "titleFa": "string",
  "level": "1-10",
  "durationSec": "number",
  "reps": "number|null",
  "instructionsFa": "string",
  "audioCueId": "string",       // اعلان صوتی مخصوص این حرکت
  "referenceImagePromptTemplate": "string", // پرامپت پایه‌ی تولید تصویر با چهره‌ی کاربر
  "generatedImageUrl": "string|null"
}
```

## ۲) جریان کار «تولید تصویر با چهره‌ی کاربر»
1. کاربر یک عکس پرتره (سلفی) در تنظیمات پروفایل آپلود می‌کند (با رضایت صریح؛ ذخیره‌سازی فقط برای این منظور).
2. برای هر حرکت، Appwrite Function به نام `generate-move-image` فراخوانی می‌شود که:
   - عکس مرجع کاربر + `referenceImagePromptTemplate` را به سرویس تولید تصویر (image-gen API) می‌فرستد.
   - خروجی را در Storage ذخیره و URL را در `generatedImageUrl` می‌نویسد.
3. تصویر تولیدشده cache می‌شود؛ فقط با تغییر عکس پروفایل یا درخواست صریح کاربر («تولید دوباره») مجدداً ساخته شود.

### نمونه‌ی پرامپت پایه برای هر حرکت (template)
```
یک تصویر آموزشی ساده و واضح از حالت بدنی [نام حرکت]، با چهره‌ای شبیه به تصویر پیوست‌شده (فقط شباهت چهره، نه کپی دقیق)،
در پوشش ورزشی محجبه و مناسب، پس‌زمینه‌ی ساده و روشن، زاویه‌ی دید کامل بدن، سبک illustration آموزشی، بدون متن روی تصویر.
```

## ۳) لیست کامل و تکمیل‌شده‌ی حرکات (برای بارگذاری اولیه در دیتابیس)

### الف) یوگا (Yoga)
1. کودک (Balasana)
2. گربه-گاو (Cat-Cow)
3. سگ رو به پایین (Downward Dog)
4. کبرا (Cobra)
5. جنگجو ۱ و ۲ (Warrior I/II)
6. درخت (Tree Pose)
7. پل (Bridge Pose)
8. نشسته پیچش ستون فقرات (Seated Spinal Twist)
9. کودک گسترده (Wide-Legged Child)
10. کبوتر (Pigeon Pose)
11. مثلث (Triangle Pose)
12. جسد/شاواسانا برای ریلکسیشن پایانی (Savasana)
13. نیم‌قایق (Half Boat)
14. گربه‌ی کشیده (Extended Cat Stretch)
15. خم‌شدن رو به جلو ایستاده (Standing Forward Bend)

### ب) ورزش عمومی/کششی (Exercise)
1. کشش گردن ۴ جهت
2. کشش شانه (Cross-body Shoulder Stretch)
3. چرخش مچ دست و پا
4. اسکوات آرام (Bodyweight Squat)
5. لانگز (Lunges)
6. پلانک (Plank) - سطح مبتدی تا پیشرفته
7. کرانچ شکم ملایم
8. کشش همسترینگ نشسته
9. حرکت پروانه (Butterfly Stretch)
10. بالا-پایین پاشنه (Calf Raises)
11. چرخش تنه ایستاده (Standing Trunk Rotation)
12. کشش ساق پا رو به دیوار
13. حرکت "سوپرمن" برای کمر
14. جامپینگ جک ملایم (برای گرم کردن)
15. کشش مچ و ساعد (برای کسانی که پشت میز/گوشی زیاد می‌نشینند)

### ج) تمرینات تنفسی (Breathing)
1. تنفس شکمی (Diaphragmatic Breathing)
2. تنفس ۴-۷-۸ (Relaxing Breath)
3. تنفس جعبه‌ای (Box Breathing 4-4-4-4)
4. تنفس بینی متناوب (Nadi Shodhana - نسخه‌ی ساده)
5. تنفس آه‌کشیدن آرام‌بخش (Sighing Breath)
6. تنفس شیر (Lion's Breath) - برای رهاسازی تنش
7. تنفس شمارشی برای تمرکز قبل از امتحان
8. تنفس آرام‌سازی قبل از خواب

### د) تکنیک‌های یادگیری (Learning Techniques) — به‌عنوان بخش مکمل در همین ماژول
1. تکنیک پومودورو (۲۵ دقیقه مطالعه / ۵ دقیقه استراحت)
2. تکرار فاصله‌دار (Spaced Repetition) برای فلش‌کارت‌ها
3. روش فاینمن (توضیح‌دادن درس با زبان ساده)
4. نقشه‌ی ذهنی (Mind Map) برای جمع‌بندی فصل
5. تست خودآزمایی قبل از خواندن مجدد (Active Recall)

> هرکدام از موارد بالا باید در `wellness_moves` با `category` و `level` مناسب ثبت شوند؛ تمرینات تنفسی و ورزشی نیاز به `audioCueId` برای شمارش دارند (بخش ۴).

## ۴) اعلان‌های صوتی برای شمارنده‌ها
- برای هر تمرین تنفسی: صدای راهنما با ریتم دقیق (مثلاً برای Box Breathing: "دم... ۱،۲،۳،۴ - نگه‌دار... ۱،۲،۳،۴ - بازدم... ۱،۲،۳،۴ - نگه‌دار...").
- برای حرکات تکراری (مثل اسکوات): شمارش صوتی تکرار با صدای زنگ کوتاه در پایان ست.
- فایل‌های صوتی از قبل ضبط‌شده (TTS فارسی) در Storage نگه‌داری و در `audioCueId` رفرنس شوند؛ پخش هم‌زمان با تایمر بصری روی صفحه.

## ۵) ایجنت طراح تصاویر مرجع سیاه‌قلم (سطح ۴ تا ۱۰)
یک بخش مجزا در اپ («مرجع نقاشی») اضافه شود:
- کاربر یک سطح مهارت (۴ تا ۱۰ از ۱۰) و یک موضوع (چهره، طبیعت، حیوان، اشیاء) انتخاب می‌کند.
- Appwrite Function به نام `generate-sketch-reference` یک پرامپت تولید تصویر می‌سازد، مثلاً:
```
یک تصویر مرجع برای تمرین نقاشی سیاه‌قلم (پنسیل)، موضوع: [موضوع]، سطح مهارت: [سطح]/۱۰
(سطح ۴ = خطوط ساده و سایه‌زنی پایه؛ سطح ۷ = سایه‌زنی میان‌رده و تناسبات دقیق‌تر؛ سطح ۱۰ = جزئیات حرفه‌ای، نور و سایه‌ی پیچیده، بافت)،
تصویر باید فقط طرح خطی/کدر سیاه و سفید باشد (نه رنگی)، مناسب کپی‌کردن با مداد روی کاغذ.
```
- تصویر تولیدشده در گالری «مرجع‌های من» ذخیره شود؛ امکان درخواست نسخه‌ی جدید با همان موضوع/سطح.

## معیار پذیرش
- [ ] حداقل ۱۵ حرکت یوگا، ۱۵ ورزش، ۸ تنفس، ۵ تکنیک یادگیری در دیتابیس اولیه بارگذاری شده باشد.
- [ ] برای هر حرکت، تصویر با چهره‌ی کاربر (در صورت وجود عکس پروفایل) تولید و کش می‌شود.
- [ ] اعلان صوتی شمارنده هماهنگ با تایمر بصری پخش می‌شود.
- [ ] بخش «مرجع نقاشی» تصویر سیاه‌قلم متناسب با سطح انتخابی تولید می‌کند.
